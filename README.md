# Inkbug Beta Mobile

Mobile-optimized React web companion for the existing Inkbug Supabase backend.

## Run locally

```bash
npm install
npm run dev
```

## Environment

Copy `.env.example` to `.env.local` if you want to override the bundled Supabase values.

## Deploy

The app is configured for static hosting on Vercel or Netlify.
